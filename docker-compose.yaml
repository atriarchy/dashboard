services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    ports:
      - 5432:5432
    environment:
      POSTGRES_DB: atriarchy
      POSTGRES_USER: atriarchy
      POSTGRES_PASSWORD: atriarchy
    volumes:
      - db:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U atriarchy"]
      interval: 1s
      timeout: 5s
      retries: 10

  s3:
    image: bitnami/minio:latest
    ports:
      - 9005:9005
      - 9006:9006
    environment:
      MINIO_ROOT_USER: atriarchy_key
      MINIO_ROOT_PASSWORD: atriarchy_secret
      MINIO_DEFAULT_BUCKETS: atriarchy:download
      MINIO_API_PORT_NUMBER: 9005
      MINIO_CONSOLE_PORT_NUMBER: 9006
    volumes:
      - s3:/bitnami/minio/data

volumes:
  db:
  s3:
